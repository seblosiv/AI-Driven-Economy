[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=app.backend
    --cov-report=term-missing:skip-covered
    --cov-report=html
    --cov-report=xml
